# GBNF schema for grok-build output
# Token-level constraints for constrained decoding (fonte: gabarito.json)

CliOutput:
  cli_spec: object
  workspace_spec: object
  validated_at: iso8601
  status: "success" | "failure"

# Constraints for the output
stop_tokens: ["</|eot_id|>"]
max_tokens: 1024

# Required fields (as per schema)
required: [cli_spec, workspace_spec]
