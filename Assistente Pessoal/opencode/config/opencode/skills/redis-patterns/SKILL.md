---
name: redis-patterns
description: In-memory cache concepts — structures per use, TTL discipline, pub/sub events (concepts only, never the server).
category: skill
model: local-forge/proposer
---

# redis-patterns

Conceitos de cache em memória: estrutura por uso (string/hash/list/set/zset/stream),
TTL disciplinado (tudo expira por desenho), pub/sub p/ eventos.
Helenizado de `redis/redis` (origem: https://github.com/redis/redis).

> PROIBIDO como dependência (doutrina R2): vault é o estado. Só conceitos.
