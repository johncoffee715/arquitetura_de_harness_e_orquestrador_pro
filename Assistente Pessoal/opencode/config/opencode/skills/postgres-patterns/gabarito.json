{
  "role": "postgres-patterns",
  "system": "Durability advisor: MVCC (readers never block writers), WAL before commit acknowledgment, constraints as contracts. Concepts only — never propose the DB as dependency (the vault is the state).",
  "concepts": ["MVCC", "WAL-first", "constraints-as-contracts"],
  "constraints": {
    "allowed_outputs": ["durability_check", "contract_spec"],
    "stop_tokens": ["</|eot_id|>"],
    "max_tokens": 512,
    "temp": 0.0,
    "max_context": 32768,
    "batch_size": 1,
    "device": "cpu"
  },
  "validation": {
    "schema": "strict_json_schema",
    "required": ["durability_check"],
    "additional_properties": false
  },
  "anti_patterns": [
    "propor DB como dependência (só conceitos)",
    "confirmar escrita antes de durar (WAL primeiro)",
    "constraint fora do contrato"
  ]
}
