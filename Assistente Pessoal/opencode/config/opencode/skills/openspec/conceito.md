# Conceito & Persona (Ontology)
---
name: openspec
description: |
  Fluid spec-driven loop: explore/propose/apply/archive change folders
  (proposal, WHEN/THEN specs, design, tasks) plus cross-repo Stores.
  Brownfield-first, iterative, no rigid phase gates.
category: skill
role: openspec
type: skill
version: 1.0.0
creation_date: 2026-09-12
author: gran-mestre-hefesto

# Helenization metadata (R77 - 3 layers)
# 1. Ontology & persona (system prompt imutável)
# 2. Gabarito/firewall (constraints & safety)
# 3. Mecânica de ignição (execution & validation)

# R75 bindings by category:
#   provider: local-forge
#   category: skill
#   model: local-forge/proposer

# R8 catalog-first:
#   - varredura: spec-kit (rígido/gates) + onp-spec-driven + cc-harness-iai cobrem
#     spec-driven; NADA cobre o ciclo fluido explore→propose→apply→archive nem Stores
#   - gap: CONFIRMED (estreito) — lifecycle fluido + Stores cross-repo via git

# R2 global:
#   - skill: openspec
#     binding: skill
#     category: skill

# R77 3-layer helenization:
#   1. Ontology & persona (system prompt imutável)
#   2. Gabarito/firewall (esquema rígido, stop_tokens, GBNF)
#   3. Mecânica de ignição (motor determinístico, validação, anti-loop)

# R28 categórico veredito:
#   Capacidades: change-lifecycle + Stores, debilidade: telemetria on-by-default no original,
#   possibilidades: archive automático pós-merge
#   Score: ≥90 (escala R34)
#   Evidência: frontmatter válido + mecanica.py smoke + gabarito JSON válido

# Provenance (helenização):
#   origin: absorvido:Fission-AI/OpenSpec
#   source: https://github.com/Fission-AI/OpenSpec
#   licenses: MIT — sem cópia literal (só lifecycle + formato WHEN/THEN + Stores)
