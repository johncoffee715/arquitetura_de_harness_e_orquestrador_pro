# Conceito & Persona (Ontology)
---
name: wigolo
description: |
  Local-first web layer over MCP/REST/SDK: multi-engine search, tiered fetch,
  crawl, structured extract, cache, find-similar, cited research, autonomous
  gather loops. $0/query, evidence with byte-pinned spans, honest degradation.
category: skill
role: wigolo
type: skill
version: 1.0.0
creation_date: 2026-09-12
author: gran-mestre-hefesto

# Helenization metadata (R77 - 3 layers)
# 1. Ontology & persona (system prompt imutável)
# 2. Gabarito/firewall (constraints & safety)
# 3. Mecânica de ignição (execution & validation)

# R75 bindings by category:
#   provider: local-forge
#   category: skill
#   model: local-forge/proposer

# R8 catalog-first:
#   - varredura: firecrawl (cloud) + browser-use (automação) + mcp-servers/fetch
#     cobrem web geral; NADA cobre web local-first $0/query com evidência pinnada
#   - gap: CONFIRMED — search/fetch/crawl/extract/research local-first via MCP

# R2 global:
#   - skill: wigolo
#     binding: skill
#     category: skill

# R77 3-layer helenization:
#   1. Ontology & persona (system prompt imutável)
#   2. Gabarito/firewall (esquema rígido, stop_tokens, GBNF)
#   3. Mecânica de ignição (motor determinístico, validação, anti-loop)

# R28 categórico veredito:
#   Capacidades: web local-first, debilidade: IP datacenter vs challenge,
#   possibilidades: proxy opt-in + cache compartilhado
#   Score: ≥90 (escala R34)
#   Evidência: frontmatter válido + mecanica.py smoke + gabarito JSON válido

# Provenance (helenização):
#   origin: absorvido:knockoutez/wigolo
#   source: https://github.com/knockoutez/wigolo
#   licenses: AGPL-3.0 — sem cópia literal (só contratos de tool + padrões)
