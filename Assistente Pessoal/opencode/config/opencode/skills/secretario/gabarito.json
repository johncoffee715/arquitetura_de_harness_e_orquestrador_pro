{
  "feature": "secretario",
  "version": "1.0.0",
  "descricao": "Firewall do SECRETARIO executivo (forja hefesto 2026-09-14): triagem semantica, registro vetorial origem=secretario, agenda, correspondencia por templates, governanca HITL; prosa livre, invencao e placebo operante proibidos.",
  "allow": {
    "tools": ["read", "grep", "glob", "bash(read-only)", "edit", "write", "tool-calling"],
    "paths": ["/mnt/dados/Assistente Pessoal/cerebro com IA/**", "/tmp/opencode/**", "/home/johncoffee/.config/opencode/skills/secretario/**"],
    "apis": ["http://127.0.0.1:9094/v1/**", "http://127.0.0.1:9097/v1/**", "http://localhost:6333/**", "http://127.0.0.1:9084/v1/**", "http://127.0.0.1:9091/**"],
    "formats": ["md", "json", "txt"],
    "sampling": {
      "temperature": 0.0,
      "top_k": 10,
      "top_p": 0.9,
      "repeat_penalty": 1.1,
      "max_tokens": 512
    }
  },
  "deny": {
    "tools": [],
    "paths": ["/etc/**", "/usr/**", "**/.env", "**/*.key", "**/*.pem", "/mnt/dados/Assistente Pessoal/modelos LLM/**"],
    "behaviors": [
      "apagar arquivo com rm — descarte so move para quarentena/secretario com motivo",
      "inventar path, metadado, fato ou conteudo de campo de template",
      "upsert de vetor placebo ou zero sem flag vetor_placebo explicita",
      "executar acao critica (mover/arquivar/descartar) sem hitl:true aprovado",
      "prosa livre ao usuario — falar so por templates preenchidos com dados reais",
      "escrever fora do vault sem autorizacao explicita",
      "loop de retry acima de 3 tentativas (falhou 3x = excecao + escalar)",
      "alterar a skill bibliotecario (reuso por import, nunca por mutacao)",
      "tocar segredos ou credenciais por qualquer motivo",
      "mascarar modo lexical_placebo como resultado semantico real"
    ]
  }
}
