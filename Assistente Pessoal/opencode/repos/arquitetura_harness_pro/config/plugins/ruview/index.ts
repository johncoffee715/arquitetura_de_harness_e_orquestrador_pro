// Helen — plugin helenizado de ruvnet/RuView — gerado por helenize_deploy.py v2 (NÃO editar)
export const HelenizeRuview = async ({ client, $ }) => {
  return {
    "session.created": async (input) => {
      // no-op leve; a origem vive no agent-registry (consultável via gran-mestre arsenal)
    },
  };
};
