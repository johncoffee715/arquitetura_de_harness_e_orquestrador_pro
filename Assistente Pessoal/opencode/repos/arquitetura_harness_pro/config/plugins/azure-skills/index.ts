// Helen — plugin helenizado de microsoft/github-copilot-for-azure — gerado por helenize_deploy.py v2 (NÃO editar)
export const HelenizeAzureSkills = async ({ client, $ }) => {
  return {
    "session.created": async (input) => {
      // no-op leve; a origem vive no agent-registry (consultável via gran-mestre arsenal)
    },
  };
};
