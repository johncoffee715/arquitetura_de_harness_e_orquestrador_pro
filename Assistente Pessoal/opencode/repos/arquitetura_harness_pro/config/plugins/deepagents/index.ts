// Helen — plugin helenizado de langchain-ai/deepagents — gerado por helenize_deploy.py v2 (NÃO editar)
export const HelenizeDeepagents = async ({ client, $ }) => {
  return {
    "session.created": async (input) => {
      // no-op leve; a origem vive no agent-registry (consultável via gran-mestre arsenal)
    },
  };
};
