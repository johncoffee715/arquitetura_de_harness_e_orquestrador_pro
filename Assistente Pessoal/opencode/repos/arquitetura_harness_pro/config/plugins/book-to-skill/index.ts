// Helen — plugin helenizado de virgiliojr94/book-to-skill — gerado por helenize_deploy.py v2 (NÃO editar)
export const HelenizeBookToSkill = async ({ client, $ }) => {
  return {
    "session.created": async (input) => {
      // no-op leve; a origem vive no agent-registry (consultável via gran-mestre arsenal)
    },
  };
};
