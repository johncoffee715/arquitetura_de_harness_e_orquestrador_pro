// Helen — plugin helenizado de bojieli/ai-agent-book — gerado por helenize_deploy.py v2 (NÃO editar)
export const HelenizeAiAgentBook = async ({ client, $ }) => {
  return {
    "session.created": async (input) => {
      // no-op leve; a origem vive no agent-registry (consultável via gran-mestre arsenal)
    },
  };
};
