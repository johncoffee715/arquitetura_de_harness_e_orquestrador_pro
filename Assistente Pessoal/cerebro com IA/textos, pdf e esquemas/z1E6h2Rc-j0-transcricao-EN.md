---
video_id: z1E6h2Rc-j0
titulo: "Destroys Google Translate? Tencent's New 440MB AI Runs 100% Offline (Hy-MT2 Review)"
url: https://www.youtube.com/watch?v=z1E6h2Rc-j0
canal: Ray Codes
publicado: 2026-09-17
duracao: 11:01
etiquetas_4selfs: [L-e, S-ca]
origem_legenda: auto-sub EN (yt-dlp 2026.08.19)
data_ingestao: 2026-09-17
cues: 653
nota_relevancia: BAIXA-MÉDIA p/ (a) — tradução edge (não visão), mas padrão GGUF/CPU + menção de pareamento com VLMs (Qwen 2.5-VL 2B/LFM 2.5) e latência sub-segundo. (b) BAIXA p/ P2 (sem vídeo/ComfyUI); útil p/ pipeline de legendas local.
---

# Transcrição EN limpa — z1E6h2Rc-j0 (dedupe, sem timestamps)

Translating private data through cloud APIs is expensive and timeconuming. That's where Tencent have released a local alternative that runs entirely offline. And today I'm going to cover all the key details, the benchmark numbers and also going to download this in my own system where it has to take a subtitle file of my previous video and convert it into Hindi and Spanish language. If you're new to the channel, welcome to build with AI series where I cover local AI free tools and models and build practical AI projects with them. And today's focus is going to be on the recently released Tencent High MT2 model, which is a new translation model that runs directly on your local machine. This model specifically focuses on high-speed [music] processing where it can translate multiple languages locally and processes documents faster than many commercial alternatives. The core architecture was built by the Tensen team, optimized specifically for instruction following and handling all of our strict rules accurately. And we can even use it for our commercial projects as the license allows full business integration and no restrictive usage at all. The model comes with different download sizes as you can go for the smallest 2 billion parameter version which keeps the processing weights very light that can run even on your CPU but they also offer a larger server variant of 30 billion parameters. And this version as well just activates 3 billion parameters on a particular task. And the memory requirement for the lightest variant is just 440 MB. So we only need half of GBTE of RAM. So it can easily run on very basic computers and you can translate across major global languages as it includes support for specific regional dialects and covers most of the standard business requirements. On the benchmark test, it shows that it can perform extremely well at following rules as it secures the top rank on the formatting benchmarks. [music] It listens really well on all of our prompt instructions. Now before going into the architecture and the benchmark numbers, let us download this in our own system where I'm going to go for the compressed EGF files which are already available on hugging phase. So for the smallest 1.8 million parameters, you have three different versions of 4bit, 6bit and 8 bit quantization. So according to your graphic card memory, you can download either of these versions. If you want a hint of how much graphic card you will be needing, you can just plug in the values in our standard formula where here let's say the billion of parameters is 1.8. So let's say around two quantization let's say if we go for the 4bit quantization it will be 2 * 4 which is 8x 81 and 1.2 for the conversational overhead. So we'll need just 1.2 GB of VRAM in our system and 1.2 GB of VRAM is very minimal so it can even run on our CPU. So that's the same quantization I'm going for where I'm going to download the 1.8b 4-bit quantization and using a custom model file I'm going to be registering that same GGF file in the amama directory. So first let's use the command to download the GGF file in a particular models folder in your same directory and here I'm using Google anti-gravity as my coding environment but you can go for any kind of VS code when surf or open code environments. So just copy this command go to my [music] terminal and paste it here. Now while the ggf is downloading let us look at the architectural pipeline how this model actually works. First of all the creators have compressed the original file which was quite large down to a size which just uses 440 MB instead of the original 3.6 6GB which led to less memory usage and also 1.x speed up in the final model generations. In model pipeline, the first step involves feeding your data where we can send standard text files or also the OCR extracted data from image streams. Next, we need to add our specific formatting rules where we can provide a custom vocabulary list and the model will lock all of these constraints in its memory. Then the fast engine of 1.8 8 billion or the 30 billion parameters will work and the final output is the clean conversational text in the exactly the translated data format that we requested and this kind of formatting and the rule enforcement gives the structured JSON files which is very useful for application interfaces as it keeps all of the code intact prevents any formatting errors in our final software and there are several practical ways where you can use this model right now for example for video editors it can translate subtitles while keeping your original timings so your captions will stay perfectly synchronized to your original video. Then you can also pair it with vision language models like Q1 3.52B or liquidi's LFM 2.5 which will take the screenshot and read the text and then this model will give you the complete translation and with the instruction following capability you can force this model to use specific terms allowing it to use hard technical words that the standard models often fail at. Then you can do deployment directly on edge devices like mobiles as the memory usage is less than half GB of RAM and the engine supports multiple platforms like Apple, Qualcomm or MediaTek and all of the sensitive project data like the code blocks and the table layouts will stay perfectly intact as this model can do the translations without corrupting any of your sensitive files. And last but not the least, everything runs locally on your system. So you pay no cloud processing fees and everything stays completely private on your own computer. And we can look at the actual testing numbers as well where [music] the model is ranking number one on the instruction following benchmarks across the 33 languages and the file within lick dialects. [music] And this tiny version is outscoring many of the cloud competitors like Microsoft translate and do APIs which makes it your perfect local body of translation. And against all of these competitors, the largest 30 billion model is scoring 94% which makes it the state-of-the-art open source model for machine learning translation. And even the 1.8 8 billion parameter is outscoring the Microsoft or do commercial APIs scoring 88% by just using less than half GB of memory making it the leader in the compact model space. Now the GGF file has downloaded in my system. So using the custom model file and this GGF file I'm going to be registering this in the Olama directory. So to register the GGF file you just need to run a single line command which will use Olama to create a new model under this name from the model file that we have defined here. So just copy this command, go to my terminal and paste it here. Now as an input, I'm going to give it a subtitles of my previous video where I'm talking about the deepseek official release about the flash 4.1 model and the whole video is around 1 minute big. So let's see how much time this model takes and also whether it is able to do the translation in my target languages of Hindi and Spanish. So in target languages I'm defining the name of language Hindi with the target code as hi in the model and similarly for Spanish the code is ES. So the input file is the subtitles English file that I have kept in this inputs folder and all the outputs should be saved in this output subfolder using the model that we have registered in the amama directory. So first I'm going to be reading the SRT file and extracting all of the text and the timestamps from it. And the prompt to the high empty model will be to translate the following text into the target language where I'm going to call this function twice according to the languages that we have defined in list and the model should only output the translated result without any additional explanations. Then I'm going to send this prompt to the local lama URL. And once I get the response, I'm going to be saving it in the output files in the output sub folder. Along with the translated text, I'm also saving the elapse time. So we can get an idea how much this model takes to generate the translation for a 1 minute video. So I'll also be saving a metric file which will have the number of blocks, the number of characters that the model has processed, what are the times it has taken and what is their average generation speeds. So we are going to save both the translated files along with the matrix.mmarkdown file. And to call this whole pipeline, you just need to go to terminal and send python main.py command. Now while the model is generating the response, let us look at the hardware metrics of how much memory you will be needing according to the different variants of model. So if you go for the highest 16- bit quantization in the 1.8 billion parameters, you will be needing 4 GB of graphic card in your system. So an entry-level GPU like RTX 3050 will be able to run this variant. But if you don't have this much of graphic card memory, you can go for the lower bit quantizations like 8 bit, 2 bit or even the 1.25 bit where the model sizes will keep on decreasing along with the memory usage for these models. And if you go for the full 13 billion which activates only 3 billion parameters, the model size will be 14 GB to load all of these 30 billions and the memory usage will be 8 GB of VRAM to load all of these activated parameters. So for running this, you will be needing a high-end GPU or even a Mac Studio. And when developers are actively integrating it with the vision language models, it is still able to give subsecond seconds latency. So even in the setup of 8 GB RAM and 4GB V RAM, we are able to achieve fast generations when we are combining the vision model with the translation version. But still there are some limitations of using this model like you will need a compatible runtime engine to start it and the highly compressed versions will require you to set up the wa integrations that we have also done today and the small model of 1.8 28 billion can handle the most of the common languages. But if you're using rare DX, you need the larger version of 30 billion. So it can provide you the better sentence flow in that rare language. Now the outputs are ready for my system. Let us first look at the metrics. So it has used the 4-bit quantization that we have registered. In total, it has executed 28 blocks and 3,000 characters. A total execution time of just 6 seconds. So we are able to achieve very high token generation speeds on a simple text conversion problem. So in both of these languages the average block latency is2 seconds per block and total elapse times are just 3 seconds and 2 seconds. [music] So now let us look at the outputs as well. So in the Hindi language it is given that.1 and for the context window it is saying tokens workflow but some words I can see like lightweight system is the same as in English it should have been ideally translated to the Hindi language but even I don't know like system how would we convert this to Hindi so for simple words it is able to generate the translations while for harder kind of words where the Hindi word would be too tough to understand it is still giving the same English words just written in Hindi then we can look at the Spanish translation as well I don't know Spanish but if you know you can read this and just let me know if this is working perfectly or not but at least it has given all the timestamps and the full video is still 1 minute 24 seconds which was the same as was the original text so it is able to perform decently well but the main power is the fast generation where all of these translations are just done in 2 to 3 making it really useful for local workflows where you want low latency and faster generations. If you also want to get started with this model and also modify this project according to your use case, just check the read me files and the code files in the first link in description and pin comments which will have the step-by-step instructions and running the translator modules along with some key ideas of where you can use this and some features that you can add on top of this project so you can easily install the model and modify the project according to your use case. Let me know in comments if you want me to cover more AI tools and models and follow the channel for more such AI builds updates and web flows.
