// Helen — plugin helenizado de vercel-labs/agent-skills — gerado por helenize_deploy.py v2 (NÃO editar)
export const HelenizeVercelAgentSkills = async ({ client, $ }) => {
  return {
    "session.created": async (input) => {
      // no-op leve; a origem vive no agent-registry (consultável via gran-mestre arsenal)
    },
  };
};
