// Helen — plugin helenizado de tirth8205/code-review-graph — gerado por helenize_deploy.py v2 (NÃO editar)
export const HelenizeCodeReviewGraph = async ({ client, $ }) => {
  return {
    "session.created": async (input) => {
      // no-op leve; a origem vive no agent-registry (consultável via gran-mestre arsenal)
    },
  };
};
