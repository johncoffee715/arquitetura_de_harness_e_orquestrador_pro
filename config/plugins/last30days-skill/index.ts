// Helen — plugin helenizado de mvanhorn/last30days-skill — gerado por helenize_deploy.py v2 (NÃO editar)
export const HelenizeLast30daysSkill = async ({ client, $ }) => {
  return {
    "session.created": async (input) => {
      // no-op leve; a origem vive no agent-registry (consultável via gran-mestre arsenal)
    },
  };
};
